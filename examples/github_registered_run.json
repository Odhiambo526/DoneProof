{
  "contract": {
    "task": "Create GitHub issue 'Auth bypass' in acme/api and assign alice",
    "postconditions": [
      {
        "id": "p1",
        "description": "Issue exists with the requested title",
        "provider": "github",
        "selector": {
          "repo": "acme/api",
          "kind": "issue",
          "number": null,
          "title": "Auth bypass"
        },
        "predicate": {"op": "eq", "path": "title", "expected": "Auth bypass"},
        "required": true
      },
      {
        "id": "p2",
        "description": "Alice is assigned",
        "provider": "github",
        "selector": {
          "repo": "acme/api",
          "kind": "issue",
          "number": null,
          "title": "Auth bypass"
        },
        "predicate": {"op": "contains", "path": "assignees", "expected": "alice"},
        "required": true
      }
    ]
  }
}
